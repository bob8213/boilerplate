# boilerplate
Copy a file into current directory.

## Usage
- Put files in a 'files' directory where the executable is
- Run boilerplate with the filename as argument
```
boilerplate file.txt
```

- You can create parameters with %?%parameterName%?% in the source file
```
Hi ! My name is %?%name%?%
```
